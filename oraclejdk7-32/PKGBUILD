# Maintainer: Damian Nowak <damian.nowak@atlashost.eu>

_major='7'
_minor='17'
_bit='32'
pkgname="oraclejdk${_major}-${_bit}"
pkgver="${_major}u${_minor}"
pkgrel='3'
epoch=
pkgdesc="Oracle JDK ${_major}"
arch=('i686' 'x86_64')
url='http://www.oracle.com/technetwork/java/javase/downloads/jdk7-downloads-1880260.html'
license=('unknown')
groups=()
depends=('lib32-glibc' 'lib32-libjpeg-turbo' 'lcms2' 'lib32-nss' 'ca-certificates-java')
# TODO: Add dependency on lib32-lcms2 once they add it to repo. https://bugs.archlinux.org/task/32994
makedepends=()
checkdepends=()
optdepends=()
provides=("java-environment=${_major}" "java-runtime=${_major}" "java-runtime-headless=${_major}" "java-web-start=${_major}")
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=("https://shur.atlashost.eu/files/jdk-$pkgver-linux-${_bit}.tar.gz")
noextract=()
md5sums=('694f9592d894b86a8a3cb56bf71768e6')

build() {
  zip="jdk-$pkgver-linux-${_bit}.tar.gz"
  cd "$srcdir"
  tar -xf "jdk-$pkgver-linux-${_bit}.tar.gz"
  rm "../$zip"
  mkdir -p 'opt/java'
  mv "jdk1.7.0_${_minor}" "$pkgname"
  mv "$pkgname" 'opt/java'
  mv 'opt' "$pkgdir"
  chown -R root:root "$pkgdir"

  cd "$pkgdir"
  mkdir -p 'etc/conf.d'
  echo "JAVA_HOME=\"/opt/java/$pkgname\"" > "etc/conf.d/$pkgname"
}
